const ipfsGateway = "https://gateway.pinata.cloud/ipfs/";
const fileCID = "your-file-cid"; // Replace with actual CID
const fileUrl = `${ipfsGateway}${fileCID}`;
console.log("Access your file at:", fileUrl);
